# ── 시크릿 (절대 커밋 금지) ──
.env
*.env
!.env.example

# ── 데이터베이스 ──
db.sqlite3
db.sqlite3-journal
# WAL 모드 부산물 — journal만 막아 두면 이 둘이 그대로 추적된다
db.sqlite3-wal
db.sqlite3-shm
*.sqlite3

# ── 수집 데이터 (개인정보/용량) ──
downloads/
uploads/
*.xlsx
*.xls

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── IDE / OS ──
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── 로그 ──
*.log

# ── PyInstaller / 빌드 산출물 ──
build/
dist/
*.exe
logs/
seibro_raw*/

# 자동 학습 티커맵 (런타임 캐시)
data/ticker_learned.json
data/krx_master.json
marketing/

# ── 백업 (용량 — 2026-08-03 db.sqlite3.bak 339MB가 커밋에 딸려 들어간 적 있음) ──
*.bak
_radar_backup/
